import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.intercede',
  appName: 'Intercede',
  webDir: 'dist/oracion-app/browser'
};

export default config;
